# XAU/USD Price Action Bot — Python Dependencies

# Core: data & numerics
numpy>=1.24.0
pandas>=2.0.0

# Configuration
PyYAML>=6.0

# Live price data (paper/live mode)
yfinance>=0.2.0
requests>=2.31.0

# Dashboard UI
streamlit>=1.32.0
plotly>=5.18.0

# Multi-agent system
psutil>=5.9.0          # CPU/memory metrics in BaseAgent heartbeat

# Optional: broker connectors (install the one you need)
# metaapi-cloud-sdk>=27.0.0  # MT5/MT4 cloud bridge — works on Linux VPS
# (OANDA uses requests which is already listed above)
